#include<bits/stdc++.h>
using namespace std;
int ORinRange(int n)
{
    int rem=n%4;
    if(rem==0)
    return n;
    else if(rem==1)
    return 1;
    else if(rem==2)
    return 3;
    else
    return 0;
}
int main()
{
    int n;cin>>n;
    cout<<ORinRange(n);
}
